import { FiltroProductPipe } from './filtro-product.pipe';

describe('FiltroProductPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroProductPipe();
    expect(pipe).toBeTruthy();
  });
});
